# Optional: API origin when the frontend is not served behind the same host as the API.
# Leave unset for local dev — Vite proxies /api to http://127.0.0.1:4000
# VITE_API_URL=https://your-api.example.com
